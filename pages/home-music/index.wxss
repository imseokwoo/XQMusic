/* pages/home-music/index.wxss */
page {
  background-color: #fafafa;
  padding: 0 20rpx;
}

/* 给轮播图设置圆角 */
/* 设置tranform=> 为了解决四个角有的不会变成圆角的情况，这是因为手机系统使用的webview的问题 */
/* 设置了模拟器可能还是直角但是手机上是正常的 */
.swiper {
  border-radius: 10rpx;
  overflow: hidden;
  transform: translateY(0);
}

.swiper-item {
  display: flex;
}

.swiper-image {
  width: 100%;
}

/* 播放工具栏 */
.play-bar-placeholder {
  height: 44px;
}
.play-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 8px;
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  height: 44px;
  background: #fafafa;
  box-shadow: 0 -1px 5px rgba(0, 0, 0, .1);
}

.play-bar .left,  .play-bar .right{
  display: flex;
  align-items: center;
}

.play-bar .left .album {
  width: 44px;
  height: 44px;
  position: relative;
  top: -8px;
  border-radius: 50%;
}

.play-bar .left .name {
  font-size: 14px;
  margin-left: 10rpx;
}

.play-bar .right .icon {
  width: 30px;
  height: 30px;
}

.play-bar .right .play {
  width: 25px;
  height: 25px;
  margin-right: 10rpx;
}

/* css3动画 */
@keyframes albumRotate {
  form {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
.album-anim {
  animation: albumRotate 18s linear infinite;
}